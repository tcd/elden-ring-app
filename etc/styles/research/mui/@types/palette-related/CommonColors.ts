/**
 * A collection of common colors.
 *
 * Use standalone interface over typeof colors/commons to enable module augmentation.
 *
 * Looks like just values for "black" & "white".
 */
export interface CommonColors {
    black: string
    white: string
}
