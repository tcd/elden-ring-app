import type { SxProps } from "@mui/material"

import {
    ThemeVars,
    cssUrl,
} from "@app/theme"

import {
    pointerOnHover,
    textShadow,
} from "../../mixins"

/** `#er__mobileLayout` */
const root: SxProps = {
    // no styles
}


export const rootStyles = {
    root,
}
