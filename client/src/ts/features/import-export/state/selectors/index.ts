import { RequestsSelectors } from "./RequestsSelectors"
import { BuildDataSelectors } from "./BuildDataSelectors"

export const ImportExportSelectors = {
    requests: RequestsSelectors,
    buildData: BuildDataSelectors,
}
