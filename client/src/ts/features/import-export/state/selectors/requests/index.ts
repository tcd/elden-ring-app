import { BitlySelectors } from "./BitlySelectors"
import { ImportDataSelectors } from "./ImportDataSelectors"

export const RequestsSelectors = {
    buildUrl: BitlySelectors,
    importData: ImportDataSelectors,
}
