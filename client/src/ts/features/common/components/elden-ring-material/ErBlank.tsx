import { Box } from "@mui/material"

import { invisibleContent } from "@app/theme"

/** Empty component that takes up display space. */
export const ErBlank = (_props: unknown): JSX.Element => <Box sx={invisibleContent} />
