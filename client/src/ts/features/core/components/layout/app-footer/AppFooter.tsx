import { AppFooterActions } from "."

export const AppFooter = (_props: unknown): JSX.Element => {
    return (
        <footer id="er__footer">
            <AppFooterActions />
        </footer>
    )
}
